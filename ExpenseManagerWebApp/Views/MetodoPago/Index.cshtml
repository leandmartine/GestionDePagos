@model IEnumerable<CasosUso.DTOs.MetodoPagoDTO>

@{
    ViewData["Title"] = "Métodos de Pago";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Métodos de Pago</h2>
    <a asp-action="Create" class="btn btn-primary">+ Nuevo Método de Pago</a>
</div>

@if (!string.IsNullOrEmpty(ViewBag.ErrorMessage as string))
{
    <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
}

<table class="table table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Detalle</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var mp in Model)
    {
        <tr>
            <td>@mp.Id</td>
            <td>@mp.Nombre</td>
            <td>@mp.Detalle</td>
            <td>
                <a asp-action="Details" asp-route-id="@mp.Id" class="btn btn-sm btn-info">Detalle</a>
                <a asp-action="Edit" asp-route-id="@mp.Id" class="btn btn-sm btn-warning">Editar</a>
                <a asp-action="Delete" asp-route-id="@mp.Id" class="btn btn-sm btn-danger">Eliminar</a>
            </td>
        </tr>
    }
    </tbody>
</table>
